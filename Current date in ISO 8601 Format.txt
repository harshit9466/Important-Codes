			   // Get the current date and time in UTC
			        ZonedDateTime utcCurrentDate = ZonedDateTime.now(ZoneOffset.UTC);
			        
			        // Corrected: Ensure proper formatting without appending the zone details
			        DateTimeFormatter formatter = DateTimeFormatter.ofPattern("yyyy-MM-dd'T'HH:mm:ss.SSS'Z'").withZone(ZoneOffset.UTC);

			        // Format to ISO8601 string
			        String curDateTime = utcCurrentDate.format(formatter);
			        
			        LoggerUtil.log("utcCurrentTime: "+curDateTime);


// Get the current date in UTC
				 ZonedDateTime utcCurrentDate = ZonedDateTime.now(ZoneOffset.UTC);

				 // Set the time to the start of the day (12 AM)
//				 ZonedDateTime startOfDay = utcCurrentDate.toLocalDate().atStartOfDay(ZoneOffset.UTC);

				 // Format the date-time to ISO8601 string without appending zone details
				 DateTimeFormatter formatter = DateTimeFormatter.ofPattern("yyyy-MM-dd'T'HH:mm:ss.SSS'Z'").withZone(ZoneOffset.UTC);
				 String iso8601CurrrentDateTimeString = utcCurrentDate.format(formatter);

				 LoggerUtil.log("iso8601CurrrentDateTimeString: " + iso8601CurrrentDateTimeString);